<head>
  <title>メモ詳細</title>
</head>

<% @memo_contents.each do |content| %>
  <h1>Title</h1>
  <h3><%= "#{h(content['title'])}" %></h3>
  <h1>Memo</h1>
  <h3><%= "#{h(content['content'])}" %></h3>

  <form method="get" action="/memos/<%= "#{content['memo_id']}" %>/edit">
    <button class="edit_memo_button" type="submit">編集</button>
  </form>

  <form method="post" action="/memos/<%= "#{content['memo_id']}" %>">
    <input type="hidden" name="_method" value="delete">
    <button class="delete_memo_button" type="submit">削除</button></p>
  </form>
<% end %>

<form method="get" action="/memos">
  <button class="jump_toppage_button" type="submit">メモ一覧へ</button></p>
</form>